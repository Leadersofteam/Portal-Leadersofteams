node_modules/
dist/
.next/
*.tsbuildinfo
.env
.env.*
!.env.example
coverage/
.turbo/
*.log
.DS_Store
apps/api/prisma/generated/
dump.rdb
# Lokalny override wdrożeniowy per-serwer (jak .env) — NIGDY nie commitować.
infra/staging.override.yml
# Artefakty Playwright (e2e)
test-results/
playwright-report/
.last-run.json
infra/prod.override.yml

# Lokalne ustawienia Claude Code — skille i CLAUDE.md commitujemy, ustawienia nie.
.claude/settings.local.json

# Uploady z lokalnych przebiegów testów — na produkcji to wolumen dockerowy.
# 48 takich plików trafiło do repo przez przypadek (S17, zastane).
apps/api/uploads/
.claude/worktrees/
